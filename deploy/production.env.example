# Non-secret settings only. Use a real domain, without https:// or a path.
CRM_DOMAIN=crm.example.com
# Use a unique tag for every release; never rebuild an existing release tag.
RELEASE_TAG=first-release
